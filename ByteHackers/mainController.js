/*var app = angular.module('citySolver', []);
app.controller('mainController', function($scope) {
    $scope.postIssue = false;
    $scope.showIssue = false;
    $scope.postIssues  = function()
        {
            $scope.postIssue = true;
        }
    $scope.showIssues = function()
        {
            $scope.showIssue = true;
            //$location.path('/')
        }
});
var app = angular.module("citySolver", ["ngRoute"]);
app.config(function($routeProvider) {
    $routeProvider
    .when("/", {
        templateUrl : "main.htm"
    })
    .when("/red", {
        templateUrl : "red.htm"
    })
    .when("/green", {
        templateUrl : "green.htm"
    })
    .when("/blue", {
        templateUrl : "blue.htm"
    });
});*/